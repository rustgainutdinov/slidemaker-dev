import React from "react";
import {WorkingSpace} from "./workingSpace";
import {Menu} from "./menu";

export const Editor = () => {
    return (
        <section>
            <Menu/>
            <WorkingSpace/>
        </section>
    );
};
